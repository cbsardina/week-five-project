<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" href="./styles.css">
    <title>Main</title>
  </head>
  <body>
<!-- ---------- HEADER ---------- -->
    <header>
      <div class="container">
        <div class="row">
          <div class="headerLeft col-md-6">
            <h1>Welcome to mystery word {{player}}!</h1>
          </div>
          <div class="headerRight col-md-6">
            <h2>You have {{count}} gueses left.</h2>
          </div>
        </div>
      </div>
    </header>
<!-- ---------- MAIN ---------- -->
    <main>
      <h1>{{#wordArr}} {{.}} {{/wordArr}}</h1>   <!-- temp to chk gm -->
      <h1 class="mysteryWord">{{#uScoreArr}} {{.}} {{/uScoreArr}}</h1>
      <form class="guessForm" action="/main" method="POST">
        <input type="text" name="guess" pattern="[A-Za-z]{1}" title="1 letter at a time. A-Z or a-z" value="">
        <button type="submit" name="guessSubmit" data-count="">Make a guess</button>
      </form>
      <h2>Your guesses: {{#badGuessArr}} {{.}} {{/badGuessArr}}</h2>
    </main>

  </body>
</html>
